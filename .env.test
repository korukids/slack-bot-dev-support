SLACK_API_TOKEN=foobared
SLACK_APP_TOKEN=foobared
CHANNEL_ID=support
# Use a dedicated Redis DB for tests so the suite never touches dev/prod data
# (the app defaults to DB 0). spec_helper flushes this DB around each example.
REDIS_URL=redis://localhost:6379/15
